[phases.build]
cmds = ["npm ci", "npm run build"]

[phases.setup]
nixPkgs = ["nodejs_20", "nginx"]

[start]
cmd = "cp /app/nginx.conf /etc/nginx/conf.d/default.conf && cp -r /app/dist/* /usr/share/nginx/html/ && nginx -g 'daemon off;'"
